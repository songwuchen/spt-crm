# 合同登记 / 合同评审 → SPT-CRM 字段差距映射

> **数据路径**：CRM **不**直连简道云。字段对照以 live schema dump（`docs/product/_jdy_*`）与 CRM 模型为准；日常增量由中间服务 `crm-integration` 拉取后经开放平台推入。

> **生成说明**：本表依据迁移指南 §2.6/§2.7、`需要文档.md`、以及 `_jdy_fields_pull_summary.md` 等字段 dump 与 CRM 现有模型交叉对照。仓库内已移除 `jdy_etl.py` / CRM 侧 jdy-wrapper 拉数代码。

## 源表单

| 名称 | app_id | entry_id | 说明 |
|------|--------|----------|------|
| 合同登记表 | `56ca77ce1efc301d279b8a4d` | `5d118dc922ba776f27472706` | 数据中心 |
| 合同评审 | `5de0b3e85600ec0006f420f2` | `5de0b58e8edfae0006cb571a` | 销售中心（签约门闸） |

---

## P0 — 一等公民（检索 / 对账 / join key）

| 简道云字段 | widget（已知） | CRM 落点 | 处置 |
|------------|----------------|----------|------|
| 合同号 | `_widget_1561431499698` | `contracts.contract_no` | **已有** |
| 图纸编号 | `_widget_1563607519120` | `contracts.drawing_no` | **需补列** |
| 对方合同号 | （标题级） | `contracts.peer_contract_no` | **需补列** |
| 单位名称 / 客户 | `_widget_1561431499614` | `customer_id` + 客户主档 | **已有**（OpenAPI）；内部创建经商机 |
| 部门 | `_widget_1561431499461` | `department_id/name` | **已有** |
| 业务人员 | `_widget_1561431499444` | `assignee_id/name` / `created_by_*` | **已有** |
| 订货日期 / 下卡日期 | `_widget_1563772783339` | `signed_date`（映射订货/下卡） | **已有**（语义近似） |
| 合同交货期 | `_widget_1561431500970` | `contracts.delivery_date` | **需补列** |
| 合同总金额 | （标题级） | `amount_total` | **已有**（OpenAPI 允许负数变动） |
| 合同状态(新增/变动) | （标题级） | `contracts.change_type` | **需补列** |
| 合同获取方式 | （标题级） | `contracts.acquire_method` | **需补列** |
| 到期/质保结束 | — | `end_date` | **已有** |

---

## P1 — 子表结构化

### 合同明细 → `contract_versions.key_clauses_json`（行数组）

| 简道云 | widget | CRM 语义 key | 处置 |
|--------|--------|--------------|------|
| 产品类型 | `_widget_1561431500162` | `product_type` | **进子表 UI** |
| 产品名称 | `_widget_1561431500376` | `name` | **已有** |
| 规格型号 | `_widget_1561431500392` | `spec` | **已有** |
| 单位 | `_widget_1561431500419` | `unit` | **已有** |
| 数量 | `_widget_1561431500458` | `qty` | **已有** |
| 单价 | `_widget_1561431500490` | `price` | **已有** |
| 总价 | `_widget_1561431500514` | `amount` | **已有** |
| 是否外币合同 | `_widget_1621411268784` | `is_fx` | **进子表 UI** |
| 外币单价 | `_widget_1621411268153` | `fx_price` | **进子表 UI** |
| 汇率 | `_widget_1621411269220` | `fx_rate` | **进子表 UI** |
| 外币总价 | `_widget_1621411268210` | `fx_amount` | **进子表 UI** |
| 电控装置 | `_widget_1561431500595` | `remark`（兼容）/ `elec_ctrl` | **进子表 UI**（保留 alias） |
| 技术参数及要求 | `_widget_1565223122750` | `standard` | **已有** |

### 收款计划 → `contracts.payment_terms_json` → `PaymentPlan`

| 简道云 | widget（已知） | CRM 语义 key | 处置 |
|--------|----------------|--------------|------|
| 款项性质 | `_widget_1561431500818` | `kind` | **已有** |
| 付款比例 | `_widget_1561431500832` | `ratio` | **已有** |
| 金额 | `_widget_1561431500855` | `amount` | **已有** |
| 到期日期 | `_widget_1661242797064` | `due_date` | **已有** |
| 说明 | `_widget_1665380027757` | `note` | **已有** |
| 是否提醒 | （标题级） | `remind` | **进子表 UI** |

---

## P2 — 扩展 / 关联（custom_fields 或他域）

| 简道云 | CRM 落点 | 处置 |
|--------|----------|------|
| 质保期限/金额 | `custom_fields_json` 或 `delivery_terms_json` | **进 custom_fields** |
| 行业/地区/应用领域/物料 | `custom_fields_json` | **进 custom_fields** |
| 是否含智能化 + 智能点 | `custom_fields_json` | **进 custom_fields** |
| 运费承担方 | `custom_fields_json` / `delivery_terms_json` | **进 custom_fields** |
| 合同/发货地址 | `delivery_terms_json` | **进 delivery_terms** |
| 验收方式/资料 | `DeliveryMilestone(accept)` + 附件 | **映射到他域** |
| 关联合同/项目评审 | 审批流 `contract_version` + 商机风险字段 | **映射到他域**（不建评审表） |
| 回款信息 / 开票信息 | `PaymentRecord` / `Invoice.source_contract_id` | **映射到他域**（详情 Tab） |
| 发货状态 | `DeliveryMilestone(ship)` / 订单行 | **映射到他域** |
| 评审 6 维风险 | `ContractVersion.risk_level` + 审批意见 | **映射到他域** |
| 保函/重量/呆滞设备 | 商机 `has_guarantee` 等 | **已有**（商机） |

## 明确不做

- 防重辅助列、到区比对辅助列 → 用 `uq_contract_tenant_no` 与系统关联替代
- CRM→简道云回写（本期）
- 独立「合同评审」实体表

---

## 同步键（由 crm-integration 推 OpenAPI，CRM 不直拉）

| 用途 | 字段 |
|------|------|
| Upsert 主键 | `contract_no`（租户内唯一，`POST /openapi/v1/contracts`） |
| 回款/发票匹配 | `contract_no` + `drawing_no` |
| 源追踪 | `custom_fields_json._src=jdy_contract`，`_src_id`，`_jdy_update_time`（中间服务写入） |
| 幂等 | `Idempotency-Key: jdy-contract-<data_id>-<cursor>` |
